no.nav.security.jwt:
  issuer.azuread:
    discoveryurl: ${AZURE_APP_WELL_KNOWN_URL}
    accepted_audience: ${AZURE_APP_CLIENT_ID}
    proxyurl: http://webproxy.nais:8088

scope:
  forarbeidsplassen: api://prod-gcp.teampam.pam-ad-api/.default
  kandidat-api: api://prod-fss.toi.rekrutteringsbistand-kandidat-api/.default

external:
  pam-ad-api.url: https://pam-ad-api.intern.nav.no/adapi
  kandidatliste-api.url: https://rekrutteringsbistand-kandidat-api.nais.adeo.no/rekrutteringsbistand-kandidat-api/rest/veileder/stilling

database:
  navn: rekrutteringsbistand
  url: ${NAIS_DATABASE_REKRUTTERINGSBISTAND_STILLING_API_REKRUTTERINGSBISTAND_DATABASE}
  brukernavn: ${NAIS_DATABASE_REKRUTTERINGSBISTAND_STILLING_API_REKRUTTERINGSBISTAND_USERNAME}
  passord: ${NAIS_DATABASE_REKRUTTERINGSBISTAND_STILLING_API_REKRUTTERINGSBISTAND_PASSWORD}

nais.cluster-name: ${NAIS_CLUSTER_NAME}
